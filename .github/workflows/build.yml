name: Build
on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build_cryptopro:
    name: Build server
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v3
      - name: get version
        id: package-version
        uses: martinbeentjes/npm-get-version-action@master
      - name: Log in to the Cntainer registry
        uses: docker/login-action@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extra metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/cryptopro
      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          secrets: |
            'GH_TOKEN=${{ secrets.GITHUB_TOKEN }}'
          build-args: 'github_token=${{ secrets.GITHUB_TOKEN }}'
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/cryptopro:v${{ steps.package-version.outputs.current-version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/cryptopro:latest
          labels: ${{ steps.meta.outputs.labels }}